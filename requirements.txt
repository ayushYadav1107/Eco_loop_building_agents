# ---------------------------------------------------------------------------
# Dashboard runtime — what `streamlit run dashboard.py` actually imports.
#
# Streamlit Community Cloud installs THIS file and nothing else, so it is kept
# to the app's real import set: the dashboard reads committed EnergyPlus
# results and never touches the simulation or the LLM.
#
#   To run the closed loop as well (main.py run-ai), install BOTH:
#       pip install -r requirements.txt -r requirements-sim.txt
# ---------------------------------------------------------------------------
streamlit>=1.32.0
plotly>=5.18.0
pandas>=2.0.0
numpy>=1.24.0
